# Used by `install-test-env.sh` To create a local testing environment.
# Configure these to match your preferred testing environment.
# By default, they mirror the hosts/credentials exposed by `wp-env`.

# Primary database (used by local install scripts).
DB_NAME=tests-wordpress
DB_HOST=tests-mysql
DB_USER=root
DB_PASSWORD=password
WP_TABLE_PREFIX=wp_
WP_URL=http://tests-wordpress
WP_DOMAIN=tests-wordpress
WP_ROOT_FOLDER=/var/www/html

ADMIN_EMAIL=admin@wp.test
ADMIN_USERNAME=admin
ADMIN_PASSWORD=password
SKIP_DB_CREATE=false

# Codeception + wp-browser configuration. Change these if you need tests to run
# against a different host (for example, a locally provisioned WordPress).
TEST_DB_NAME=tests-wordpress
TEST_DB_HOST=tests-mysql
TEST_DB_USER=root
TEST_DB_PASSWORD=password
TEST_WP_TABLE_PREFIX=wp_
TEST_WP_DOMAIN=tests-wordpress
TEST_WP_URL=http://tests-wordpress
TEST_WP_ROOT_FOLDER=/var/www/html

TEST_ADMIN_EMAIL=admin@wp.test
TEST_ADMIN_USERNAME=admin
TEST_ADMIN_PASSWORD=password
TEST_ADMIN_PATH=/wp-admin

# Whether wpunit should run in multisite mode.
MULTISITE=0
