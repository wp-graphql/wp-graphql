import {
  cleanup,
  fireEvent,
  render,
  screen,
  waitFor,
  act,
} from "@testing-library/react";

describe("AppContext", () => {
  test("should render", () => {
    expect(true).toBe(true);
  });
});
