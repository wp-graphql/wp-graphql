<?xml version="1.0"?>
<ruleset name="Coding standards for WPGraphQL">
	<!-- Set a description for this ruleset -->
	<description>Coding standards that will be run on WPGraphQL.</description>

	<!-- Check against minimum WP version. -->
	<config name="minimum_supported_wp_version" value="4.7"/>

	<!-- Load WordPress Coding standards -->
	<rule ref="WordPress-Docs"/>
	<rule ref="WordPress-Extra"/>
	<rule ref="WordPress.WP.I18n">
		<properties>
			<property name="text_domain" value="wp-graphql,graphql" />
		</properties>
	</rule>

	<!-- Disallow long array syntax -->
	<rule ref="Generic.Arrays.DisallowLongArraySyntax"/>

	<!-- Allow short array syntax -->
	<rule ref="Generic.Arrays.DisallowShortArraySyntax.Found">
		<exclude name="Generic.Arrays.DisallowShortArraySyntax.Found"/>
	</rule>

	<!-- Will check the code compatiblity for PHP no less than version 7.0 -->
	<rule ref="PHPCompatibilityWP"/>
	<config name="testVersion" value="7.0-"/>

	<!-- What to exclude -->
	<exclude-pattern>*/node_modules/*</exclude-pattern>
	<exclude-pattern>*/vendor/*</exclude-pattern>
	<exclude-pattern>*/tests/*</exclude-pattern>

	<!-- What to scan -->
	<file>src</file>

	<!-- Enable colors in report -->
	<arg name="colors"/>

	<!-- Add source codes in the report -->
	<arg value="s"/>

	<!-- Remove the class naming error temporarily -->
	<rule ref="WordPress.Files.FileName">
		<properties>
			<property name="strict_class_file_names" value="false"/>
		</properties>
	</rule>

	<rule ref="WordPress.Files.FileName.NotHyphenatedLowercase">
		<severity>0</severity>
	</rule>
</ruleset>
